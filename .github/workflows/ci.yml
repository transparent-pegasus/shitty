name: CI

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      darwin_tests:
        description: Run the Darwin build and test shards
        type: boolean
        default: false
      sandboxed:
        description: Audit declared build inputs under strace
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  build:
    name: Build Linux
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.build \
            2>&1 | tee "$RUNNER_TEMP/build.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/build.log" \
            --title "Build Linux" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.build"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-log
          path: ${{ runner.temp }}/build.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-alpine:
    name: Tests Alpine
    needs: build
    runs-on: ubuntu-24.04
    container: alpine:3.24
    timeout-minutes: 90
    steps:
      - name: Prepare Alpine for GitHub Actions
        run: apk add --no-cache gcompat git

      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install the stock Alpine toolchain
        run: |
          apk add --no-cache \
            bash gcc g++ musl-dev linux-headers make python3 zstd perl perl-doc pkgconf \
            diffutils procps ragel glslang rsvg-convert ncurses-dev \
            brotli-dev fontconfig-dev freetype-dev harfbuzz-dev simdutf-dev \
            libxcb-dev libxkbcommon-dev wayland-dev wayland-protocols \
            vulkan-headers vulkan-loader-dev mesa-vulkan-swrast xvfb

      - name: Run full test suite with GCC and musl
        env:
          CC: gcc
          CXX: g++
          PLT_X11_TEST_REQUIRED: "1"
          SHITTY_TEST_VULKAN_REQUIRED: "1"
        run: |
          set -o pipefail
          export VK_DRIVER_FILES="$(find /usr/share/vulkan/icd.d -name 'lvp_icd*.json' -print -quit)"
          export VK_ICD_FILENAMES="$VK_DRIVER_FILES"
          ./build test 2>&1 | tee "$RUNNER_TEMP/tests-alpine.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-alpine.log" \
            --title "Tests Alpine" \
            --reproduce "run the Tests Alpine steps from .github/workflows/ci.yml in an alpine:3.24 container"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-alpine-log
          path: ${{ runner.temp }}/tests-alpine.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-fedora:
    name: Tests Fedora
    needs: build
    runs-on: ubuntu-24.04
    container: fedora:44
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install the stock Fedora toolchain
        # Retry: the Fedora mirrors intermittently fail mid-download (curl 23,
        # "Failed writing received data to disk", usually on a zchunk delta), so
        # disable zchunk, prefer the fastest mirror, and try a few times.
        run: |
          for attempt in 1 2 3 4 5; do
            if dnf -y --setopt=install_weak_deps=False \
                --setopt=retries=10 --setopt=timeout=60 \
                --setopt=fastestmirror=True --setopt=zchunk=False \
                install \
                gcc-c++ make git python3 perl pkgconf-pkg-config diffutils procps-ng \
                ragel glslang librsvg2-tools ncurses-devel \
                freetype-devel fontconfig-devel harfbuzz-devel \
                libxcb-devel libxkbcommon-devel \
                vulkan-headers vulkan-loader-devel mesa-vulkan-drivers \
                wayland-devel wayland-protocols-devel xorg-x11-server-Xvfb; then
              exit 0
            fi
            echo "dnf install failed (attempt $attempt); cleaning and retrying" >&2
            dnf clean all || true
            sleep 15
          done
          echo "dnf install failed after 5 attempts" >&2
          exit 1

      - name: Run full test suite with GCC and Ragel 7
        env:
          CC: gcc
          CXX: g++
          PLT_X11_TEST_REQUIRED: "1"
        run: |
          set -o pipefail
          ./build test 2>&1 | tee "$RUNNER_TEMP/tests-fedora.log"

      - name: Smoke-test X11 with Vulkan lavapipe
        env:
          CC: gcc
          CXX: g++
        run: |
          set -eu
          set -o pipefail

          ./build st 2>&1 | tee "$RUNNER_TEMP/x11-build.log"

          export VK_DRIVER_FILES="$(find /usr/share/vulkan/icd.d -name 'lvp_icd*.json' -print -quit)"
          export VK_ICD_FILENAMES="$VK_DRIVER_FILES"
          test -n "$VK_DRIVER_FILES"

          display_file="$RUNNER_TEMP/xvfb-display"
          xvfb_log="$RUNNER_TEMP/xvfb.log"
          x11_log="$RUNNER_TEMP/x11-smoke.log"
          xvfb_pid=
          cleanup() {
            if test -n "$xvfb_pid"; then
              kill "$xvfb_pid" 2>/dev/null || true
              wait "$xvfb_pid" 2>/dev/null || true
            fi
          }
          trap cleanup EXIT

          Xvfb \
            -displayfd 3 \
            -screen 0 1024x768x24 \
            -nolisten tcp \
            >"$xvfb_log" 2>&1 \
            3>"$display_file" &
          xvfb_pid=$!

          attempt=0
          while test "$attempt" -lt 100 && ! test -s "$display_file"; do
            if ! kill -0 "$xvfb_pid" 2>/dev/null; then
              cat "$xvfb_log"
              exit 1
            fi
            sleep 0.1
            attempt=$((attempt + 1))
          done
          if ! test -s "$display_file"; then
            cat "$xvfb_log"
            exit 1
          fi

          export DISPLAY=":$(cat "$display_file")"
          unset WAYLAND_DISPLAY WAYLAND_SOCKET

          if ! timeout --signal=TERM --kill-after=5s 30s ./st \
              -vulkanInfo \
              -geometry 20x5 \
              -e /bin/sh -c 'printf "Vulkan X11 smoke\n"; sleep 2' \
              >"$x11_log" 2>&1; then
            cat "$x11_log"
            cat "$xvfb_log"
            exit 1
          fi
          cat "$x11_log"

          grep -q '^Vulkan device: llvmpipe' "$x11_log"
          grep -q '^Vulkan presentation:' "$x11_log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-fedora.log" \
            --title "Tests Fedora" \
            --reproduce "run the Tests Fedora steps from .github/workflows/ci.yml in a fedora:44 container"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-fedora-log
          path: |
            ${{ runner.temp }}/tests-fedora.log
            ${{ runner.temp }}/x11-build.log
            ${{ runner.temp }}/x11-smoke.log
            ${{ runner.temp }}/xvfb.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  sandboxed-0:
    name: Sandboxed 0/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.sandboxed }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Audit declared build inputs
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.sandboxed-0-of-5 \
            2>&1 | tee "$RUNNER_TEMP/sandboxed-0-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/sandboxed-0-of-5.log" \
            --title "Sandboxed 0/5" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.sandboxed-0-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandboxed-0-of-5-log
          path: ${{ runner.temp }}/sandboxed-0-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  sandboxed-1:
    name: Sandboxed 1/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.sandboxed }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Audit declared build inputs
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.sandboxed-1-of-5 \
            2>&1 | tee "$RUNNER_TEMP/sandboxed-1-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/sandboxed-1-of-5.log" \
            --title "Sandboxed 1/5" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.sandboxed-1-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandboxed-1-of-5-log
          path: ${{ runner.temp }}/sandboxed-1-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  sandboxed-2:
    name: Sandboxed 2/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.sandboxed }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Audit declared build inputs
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.sandboxed-2-of-5 \
            2>&1 | tee "$RUNNER_TEMP/sandboxed-2-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/sandboxed-2-of-5.log" \
            --title "Sandboxed 2/5" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.sandboxed-2-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandboxed-2-of-5-log
          path: ${{ runner.temp }}/sandboxed-2-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  sandboxed-3:
    name: Sandboxed 3/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.sandboxed }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Audit declared build inputs
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.sandboxed-3-of-5 \
            2>&1 | tee "$RUNNER_TEMP/sandboxed-3-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/sandboxed-3-of-5.log" \
            --title "Sandboxed 3/5" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.sandboxed-3-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandboxed-3-of-5-log
          path: ${{ runner.temp }}/sandboxed-3-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  sandboxed-4:
    name: Sandboxed 4/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.sandboxed }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Audit declared build inputs
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.sandboxed-4-of-5 \
            2>&1 | tee "$RUNNER_TEMP/sandboxed-4-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/sandboxed-4-of-5.log" \
            --title "Sandboxed 4/5" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.sandboxed-4-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sandboxed-4-of-5-log
          path: ${{ runner.temp }}/sandboxed-4-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  build-darwin:
    name: Build Darwin
    needs: build
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.build \
            2>&1 | tee "$RUNNER_TEMP/build-darwin.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/build-darwin.log" \
            --title "Build Darwin" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.build"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-darwin-log
          path: ${{ runner.temp }}/build-darwin.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  nix-flake-linux:
    name: Nix Linux
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#packages.x86_64-linux.default \
            2>&1 | tee "$RUNNER_TEMP/nix-flake-linux.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/nix-flake-linux.log" \
            --title "Nix Linux" \
            --reproduce "nix build -L --no-link .#packages.x86_64-linux.default"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nix-flake-linux-log
          path: ${{ runner.temp }}/nix-flake-linux.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  nix-flake-darwin:
    name: Nix Darwin
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Build flake package
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#packages.aarch64-darwin.default \
            2>&1 | tee "$RUNNER_TEMP/nix-flake-darwin.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/nix-flake-darwin.log" \
            --title "Nix Darwin" \
            --reproduce "nix build -L --no-link .#packages.aarch64-darwin.default"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: nix-flake-darwin-log
          path: ${{ runner.temp }}/nix-flake-darwin.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-0:
    name: Tests Darwin 0/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.darwin_tests }}
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 0/5 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-0-of-5 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-0-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-0-of-5.log" \
            --title "Tests Darwin 0/5" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-0-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-0-of-5-log
          path: ${{ runner.temp }}/tests-darwin-0-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-1:
    name: Tests Darwin 1/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.darwin_tests }}
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 1/5 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-1-of-5 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-1-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-1-of-5.log" \
            --title "Tests Darwin 1/5" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-1-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-1-of-5-log
          path: ${{ runner.temp }}/tests-darwin-1-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-2:
    name: Tests Darwin 2/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.darwin_tests }}
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 2/5 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-2-of-5 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-2-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-2-of-5.log" \
            --title "Tests Darwin 2/5" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-2-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-2-of-5-log
          path: ${{ runner.temp }}/tests-darwin-2-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-3:
    name: Tests Darwin 3/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.darwin_tests }}
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 3/5 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-3-of-5 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-3-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-3-of-5.log" \
            --title "Tests Darwin 3/5" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-3-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-3-of-5-log
          path: ${{ runner.temp }}/tests-darwin-3-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-darwin-4:
    name: Tests Darwin 4/5
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.darwin_tests }}
    needs: build-darwin
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run test partition 4/5 on Darwin
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.aarch64-darwin.tests-4-of-5 \
            2>&1 | tee "$RUNNER_TEMP/tests-darwin-4-of-5.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-darwin-4-of-5.log" \
            --title "Tests Darwin 4/5" \
            --reproduce "nix build -L --no-link .#checks.aarch64-darwin.tests-4-of-5"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-darwin-4-of-5-log
          path: ${{ runner.temp }}/tests-darwin-4-of-5.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  coverage:
    name: Coverage
    needs: build
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run full test suite with coverage
        run: |
          set -o pipefail
          nix build -L \
            --out-link "$RUNNER_TEMP/coverage-result" \
            .#checks.x86_64-linux.coverage \
            2>&1 | tee "$RUNNER_TEMP/coverage.log"

      - name: Prepare coverage report
        run: |
          mkdir -p "$RUNNER_TEMP/coverage-report"
          cp -a "$RUNNER_TEMP/coverage-result/." \
            "$RUNNER_TEMP/coverage-report/"
          {
            echo "## LLVM coverage"
            echo
            echo '```text'
            cat "$RUNNER_TEMP/coverage-report/summary.txt"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload browsable coverage report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-report
          path: ${{ runner.temp }}/coverage-report
          if-no-files-found: error
          retention-days: 14
          compression-level: 9

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          use_oidc: true
          files: ${{ runner.temp }}/coverage-report/coverage.info,${{ runner.temp }}/coverage-report/wayland-coverage.info
          disable_search: true
          fail_ci_if_error: true
          name: linux-clang
          verbose: true

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/coverage.log" \
            --title "Coverage" \
            --reproduce "nix build -L -o result-coverage .#checks.x86_64-linux.coverage"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-log
          path: ${{ runner.temp }}/coverage.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-asan:
    name: Tests ASan
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run full test suite with ASan
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.tests-asan \
            2>&1 | tee "$RUNNER_TEMP/tests-asan.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-asan.log" \
            --title "Tests ASan" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.tests-asan"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-asan-log
          path: ${{ runner.temp }}/tests-asan.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9

  tests-ubsan:
    name: Tests UBSan
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          enable_kvm: false

      - name: Run full test suite with UBSan
        run: |
          set -o pipefail
          nix build -L --no-link \
            .#checks.x86_64-linux.tests-ubsan \
            2>&1 | tee "$RUNNER_TEMP/tests-ubsan.log"

      - name: Summarize failure
        if: failure()
        run: |
          python3 dev/ci_report.py \
            --log "$RUNNER_TEMP/tests-ubsan.log" \
            --title "Tests UBSan" \
            --reproduce "nix build -L --no-link .#checks.x86_64-linux.tests-ubsan"

      - name: Upload failure log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tests-ubsan-log
          path: ${{ runner.temp }}/tests-ubsan.log
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 9
